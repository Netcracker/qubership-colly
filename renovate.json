{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended"],
  "enabledManagers": ["dockerfile", "maven"],
  "lockFileMaintenance": {
    "enabled": false
  },
  "dependencyDashboard": true,
  "prConcurrentLimit": 1,
  "branchConcurrentLimit": 1,
  "packageRules": [
    {
      "description": "Maven: semver patch only — disable minor and major",
      "matchManagers": ["maven"],
      "matchUpdateTypes": ["minor", "major"],
      "enabled": false
    },
    {
      "description": "Single PR grouping Dockerfile and Maven updates",
      "matchManagers": ["dockerfile", "maven"],
      "groupName": "dependency updates"
    }
  ]
}
