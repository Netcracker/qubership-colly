---
apiVersion: v1
kind: Service
metadata:
  annotations:
    app.quarkus.io/quarkus-version: 3.27.2
  labels:
    name: {{ .Values.colly.envgeneInventoryService.serviceName }}
    app.kubernetes.io/name: {{ .Values.colly.envgeneInventoryService.serviceName }}
    app.kubernetes.io/part-of: qubership-colly
    app.kubernetes.io/managed-by: {{ .Values.MANAGED_BY }}
  name: {{ .Values.colly.envgeneInventoryService.serviceName }}
spec:
  ports:
    - name: web
      port: 8080
      protocol: TCP
      targetPort: {{ .Values.colly.envgeneInventoryService.ports.http }}
  selector:
    name: {{ .Values.colly.envgeneInventoryService.serviceName }}
  type: ClusterIP
