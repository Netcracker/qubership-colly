---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/name: {{ .Values.colly.envgeneInventoryService.serviceName }}
    app.kubernetes.io/version: {{ .Chart.Version }}
    name: {{ .Values.colly.envgeneInventoryService.serviceName }}
  name: {{ .Values.colly.envgeneInventoryService.serviceName }}
spec:
  replicas: 1
  selector:
    matchLabels:
      name: {{ .Values.colly.envgeneInventoryService.serviceName }}
  template:
    metadata:
      annotations:
        app.quarkus.io/quarkus-version: 3.18.2
      labels:
        name: {{ .Values.colly.envgeneInventoryService.serviceName }}
        app.kubernetes.io/name: {{ .Values.colly.envgeneInventoryService.serviceName }}
        app.kubernetes.io/version: {{ .Chart.Version }}
    spec:
      containers:
        - env:
            - name: KUBERNETES_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: QUARKUS_REDIS_HOSTS
              value: {{ .Values.colly.envgeneInventoryService.redis.hosts }}
            - name: QUARKUS_REDIS_USERNAME
              valueFrom:
                secretKeyRef:
                  key: username
                  name: {{ .Values.colly.envgeneInventoryService.serviceName }}-secret
            - name: QUARKUS_REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  key: password
                  name: {{ .Values.colly.envgeneInventoryService.serviceName }}-secret
            - name: COLLY_EIS_PROJECT_REPO_URL
              value: {{ .Values.colly.envgeneInventoryService.projectRepoUrl }}
            - name: COLLY_EIS_GIT_TOKEN
              valueFrom:
                secretKeyRef:
                  key: gitToken
                  name: {{ .Values.colly.envgeneInventoryService.serviceName }}-secret
            - name: COLLY_EIS_CRON_SCHEDULE
              value: {{ .Values.colly.envgeneInventoryService.cronSchedule }}
            - name: QUARKUS_OIDC_AUTH_SERVER_URL
              value: {{ .Values.colly.envgeneInventoryService.idp.url }}
            - name: QUARKUS_OIDC_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  key: idpClientId
                  name: {{ .Values.colly.envgeneInventoryService.serviceName }}-secret
            - name: QUARKUS_OIDC_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  key: idpClientSecret
                  name: {{ .Values.colly.envgeneInventoryService.serviceName }}-secret
            - name: QUARKUS_OIDC_TLS_VERIFICATION
              value: none
          image: {{ .Values.colly.envgeneInventoryService.image }}
          imagePullPolicy: Always
          name: qubership-colly
          ports:
            - containerPort: {{ .Values.colly.envgeneInventoryService.ports.http }}
              name: web
              protocol: TCP
